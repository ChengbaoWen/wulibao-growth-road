server:
  port: 80
  tomcat:
    uri-encoding: UTF-8

spring:
  application:
    name: wulibao-growth-road
  datasource:
    url: jdbc:mysql://127.0.0.1/jsoup?useSSL=false&useUnicode=true&characterEncoding=utf-8&nullNamePatternMatchesAll=true&allowMultiQueries=true
    username: root
    password: Fingard1!
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource



mybatis-plus:
  mapper-locations: classpath:mapper/*/*.xml        # 注意：一定要对应mapper映射xml文件的所在路径
  type-aliases-package: com.wulibao.**.model.entity   # 注意:此项对应实体的包路径
